CREATE TABLE "flow1" (
    "id" INTEGER NOT NULL,
    "itemType" TEXT NOT NULL,
    "label" TEXT NOT NULL,
    "desc" TEXT NOT NULL,
    "isActive" TEXT NOT NULL,
    PRIMARY KEY ("id")
);

CREATE TABLE "flow2" (
    "id" INTEGER NOT NULL,
    "itemType" TEXT NOT NULL,
    "label" TEXT NOT NULL,
    "desc" TEXT NOT NULL,
    "isActive" TEXT NOT NULL,
    PRIMARY KEY ("id")
);